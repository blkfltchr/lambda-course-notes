webpackJsonp([0x81b8806e4260],{335:function(e,t){e.exports={data:{site:{siteMetadata:{title:"Lambda Course Notes",desc:"Notes on User Interface, Front end, Back end, and Computer Science"}},jumbotron:{sizes:{base64:"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABQAAAAGCAIAAABM9SnKAAAACXBIWXMAAAsTAAALEwEAmpwYAAAAX0lEQVQY02OoEBCslZOrEhEpYWQsZWUtZWEhHjFUiYlV8vOXc3KWMDOTpBOkuUZOrk5FpYyDo4REnSDN1RISFfz8FTw8ZNksLl4pIFDBy1vCxESy5lopqQopKTLcDEQAHOI8+/ozx4MAAAAASUVORK5CYII=",aspectRatio:3.2,src:"/gatsby/static/open-door-bg-689d7036e6c5e9fc349027dfbe074323-ab24c.png",srcSet:"/gatsby/static/open-door-bg-689d7036e6c5e9fc349027dfbe074323-310d9.png 310w,\n/gatsby/static/open-door-bg-689d7036e6c5e9fc349027dfbe074323-63c25.png 620w,\n/gatsby/static/open-door-bg-689d7036e6c5e9fc349027dfbe074323-ab24c.png 1240w,\n/gatsby/static/open-door-bg-689d7036e6c5e9fc349027dfbe074323-634e9.png 1600w",sizes:"(max-width: 1240px) 100vw, 1240px"}},allMarkdownRemark:{edges:[{node:{id:"/Users/blakefletcher/projects/my-personal-blog/src/posts/react-week-1-day-1.md absPath of file >>> MarkdownRemark",frontmatter:{title:"How it works: React Week 1 Day 1 - Functional components I",date:"June 11 2018"},fields:{slug:"/notes/react-week-1-day-1/"},html:'<p>React is a UI library that is used in various forms to create complex, rich user interfaces. It powers user experiences in a way that is decoupled and unconcerned when and where the data is received.</p>\n<!-- end -->\n<p>Objectives:</p>\n<ul>\n<li>use an html file set up a react environment and mount an application to a target DOM element using React and ReactDOM.render</li>\n<li>use JSX to mount a DOM element and display that element in a web browser</li>\n<li>implement a functional component to render a piece of UI to the screen</li>\n</ul>\n<h2>Setting up a React environment using a single HTML file</h2>\n<p>React takes what we have written, and passes it to this function called <code>React.createElement()</code> which turns our code into DOM elements with all of their appropriate handlers, styles and attributes laid out.</p>\n<p>What we get with react is this idea of <code>composability</code> because all we have to do is think about React elements as smaller pieces of UI, and build out those small pieces of UI in these code blocks we call <code>components</code>.</p>\n<p>Everything you do in react will done through this <code>createElement()</code> method however, as you’ll see in the coming material,this has now been abstracted away from us and we now have the ability to use this new technology called <code>JSX</code> to write out our react elements.</p>\n<ol>\n<li>Create an <code>index.html</code> file with the following scaffolding</li>\n</ol>\n<pre><code>&#x3C;!DOCTYPE html>\n&#x3C;html lang="en">\n\n&#x3C;head>\n  &#x3C;title>Hello World React&#x3C;/title>\n&#x3C;/head>\n\n&#x3C;body>\n  &#x3C;div id="target">&#x3C;/div>\n&#x3C;/body>\n\n&#x3C;script>\n  // Our app goes in between these script tags\n&#x3C;/script>\n\n&#x3C;/html>\n</code></pre>\n<ol start="2">\n<li>Just before the script tags, add in our packages that we need</li>\n</ol>\n<pre><code>&#x3C;script crossorigin src="https://unpkg.com/react@16/umd/react.development.js">&#x3C;/script>\n&#x3C;script crossorigin src="https://unpkg.com/react-dom@16/umd/react-dom.development.js">&#x3C;/script>\n\n&#x3C;script>\n  // Our app goes in between these script tags\n&#x3C;/script>\n</code></pre>\n<ol start="3">\n<li>Create a DOM element using <code>React.createElement()</code> and make an <code>h1</code> element with a <code>class="heading"</code> and some child text <code>Hello world</code> </li>\n</ol>\n<pre><code>&#x3C;script>\nconst myElement = React.createElement(\'h1\', {className:\'heading\'}, \'Hello World\');\n&#x3C;/script>\n</code></pre>\n<ol start="4">\n<li>Use the <code>ReactDOM</code> library to target where our application will live, and inject our newly created <code>myElement</code> element using the <code>.render</code> method</li>\n</ol>\n<pre><code>&#x3C;script>\nconst myElement = React.createElement(\'h1\', {className:\'heading\'}, \'Hello World\');\nReactDOM.render(myElement, document.getElementById(\'target\'));\n&#x3C;/script>\n</code></pre>\n<p>Notice we are passing our <code>myElement</code> as the first argument to render and we’re selecting our <code>&#x3C;div id="target">&#x3C;/div></code>. For every single react application you’ll ONLY DO THIS ONE TIME for a single application. Everything else well be built out as a child of this.</p>\n<h3>Challenge</h3>\n<p>Steps to complete this assignment</p>\n<ul>\n<li>create an <code>index.html</code> file</li>\n<li>scaffold out a regular html page and add a <code>&#x3C;div id="target">&#x3C;/div></code> as the container for your application to live.</li>\n<li>pull in the React and ReactDOM packages via CDN/UNPKG</li>\n<li>create a script tag and add the attribute <code>type = “text/javascript”</code> so that you can write your react application inline</li>\n<li>inside your script tag, create your myElement variable and set it equal to the React.createElement() function: <code>const myElement = React.createElement()</code></li>\n<li>Decide what type of element you want rendered to the screen, and what attributes (should have at least 1 className) you want on your element and what children text you’d like your element to display. Pass multiple strings to as children and see what happens.</li>\n<li>finally, mount your element to your wrapper div using <code>ReactDOM.render</code></li>\n</ul>\n<h2>Using JSX to mount a DOM element and display it in a web browser</h2>\n<p>We actually could build out an application using solely the <code>createElement</code> method, but eventually it’ll get really complex, and hard to reason about, as our application scales. This is why React gave us an abstraction on top of this method, called <code>JSX</code>.</p>\n<p><a href="https://reactjs.org/docs/introducing-jsx.html#jsx-represents-objects" title="ReactJS docs: JSX represents objects">React takes your JSX and boils it down through React.createElement() calls</a> that is why it’s important to understand a little bit about this particular method.</p>\n<ol>\n<li>Use something like <code>Babel</code> transpile your JSX into a regular version of JavaScript so that Chrome can read it. To get <code>babel-standalone</code> included into our html file for now, we can simply use the <code>UNPKG</code> cdn to retrieve it.</li>\n</ol>\n<pre><code>&#x3C;script crossorigin src="https://unpkg.com/babel-standalone@6.26.0/babel.js">&#x3C;/script>\n</code></pre>\n<ol start="2">\n<li>Refactor our <code>myElement</code> variable to use <code>JSX</code> instead of <code>React.createElement()</code></li>\n</ol>\n<pre><code>const myElement = React.createElement(\'h1\', {className:\'heading\'}, \'Hello World\');\n\n// becomes\n\nconst myElement = &#x3C;h1 className="heading" >Hello World&#x3C;/h1>;\n</code></pre>\n<h3>Challenge</h3>\n<p>From scratch, build out a React application within an HTML file that will mount an header with your name, age and location. Use a <code>&#x3C;style></code> tag to add some CSS and make your profile you just built look nice and pretty.</p>\n<h2>Implementing a functional component to render a piece of UI to the screen</h2>\n<p>Everything we’ll do in React will revolve around components. There are different types of components that we can use, and we’re going to start with the simplest type, the <code>Functional Component</code>. Simply put, we can use basic functions as components. These functions will simply return out some sort of JSX. Which will then be rendered out to the Browser.</p>\n<p>Our most basic of components looks like this. It looks exactly like an anonymous arrow function that we’ve named BasicComponent. In fact, that is literally what this is.</p>\n<pre><code>const BasicComponent = () => &#x3C;h1>Hello World&#x3C;/h1>;\n</code></pre>\n<ol>\n<li>Create a wrapper element <code>App</code> and pass it in to <code>ReactDOM.render</code></li>\n</ol>\n<pre><code>const App = () => (\n  &#x3C;div>\n   // Our components go in between these parent div tags\n  &#x3C;/div>\n);\n\nReactDOM.render(&#x3C;App />, document.getElementById("root"));\n</code></pre>\n<ol start="2">\n<li>Build out some reusable functional components</li>\n</ol>\n<pre><code>const Header = () => {\n  return &#x3C;h1>Hello From React&#x3C;/h1>;\n};\n\nconst BasicInput = () => {\n  return &#x3C;input type="text" placeholder="Change the world, one input at a time" />;\n};\n\nconst BasicButton = () => {\n  return &#x3C;button>Click me I\'m a button!&#x3C;/button>;\n};\n</code></pre>\n<ol start="3">\n<li>Pass the components into our wrapper <code>&#x3C;App/></code> component and Capitalize the Components</li>\n</ol>\n<pre><code>const App = () => (\n  &#x3C;div>\n    &#x3C;Header />\n    &#x3C;BasicInput />\n    &#x3C;BasicButton />\n  &#x3C;/div>\n);\n</code></pre>\n<h3>Challenge</h3>\n<p>Build out a few pieces of composed components from scratch.</p>\n<p>This is a <a href="https://devhints.io/react" title="by DEVHINTS.IO">React Cheat Sheet</a> and below is another</p>\n<p><img src="https://ibin.co/4BGcJlgplMcW.png" alt="React Cheat Sheet" title="React Cheat Sheet"></p>',excerpt:"\nReact is a UI library that is used in various forms to create complex, rich user interfaces. It powers user experiences in a way that is decoupled and unconcerned when and where the data is received.\n\n"}},{node:{id:"/Users/blakefletcher/projects/my-personal-blog/src/posts/react-week-1-day-2.md absPath of file >>> MarkdownRemark",frontmatter:{title:"How it works: React Week 1 Day 2: Functional components II",date:"June 12 2018"},fields:{slug:"/notes/react-week-1-day-2/"},html:'<p>The way that data flows throughout a React app is crucial to understand when it comes to developing programs built in React. In React, when we pass our data around it’s passed around using State and Props. </p>\n<!-- end -->\n<p>Objectives:</p>\n<ul>\n<li>describe props and how data flows in a reacy application</li>\n<li>use props to dynamically pass data to a React Component making it reusable in different contexts</li>\n<li>demonstrate the ability to implement nested functional components</li>\n</ul>\n<h2>Describing props and how data flows in a react application</h2>\n<p>In React, when we pass our data around it’s passed around using State and Props. The direction in which data flows is top to bottom.</p>\n<p>Props are immutable (or Read only) meaning we cannot mutate props in any fashion. We simple receive them in our components and use them to display data to the user. Remember the idea here is to build out User Interfaces. How do we interface with our users, and Props are that data that we pass around that our users will interact with.</p>\n<p>Take the following example:</p>\n<pre><code>const MyComponentsWithProps = props => {\n    return &#x3C;h1>Hello, my name is {props.name}&#x3C;/h1>;\n};\n</code></pre>\n<p>When we use the component above, we’ll pass it some props so that we can print our name using that component. Because of reusability, I can now use this component in multiple instances:</p>\n<pre><code>&#x3C;MyComponentsWithProps name="Fred" />\n</code></pre>\n<p>Displays "Hello, my name is Fred." And we can use it in other instances.</p>\n<pre><code>&#x3C;MyComponentsWithProps name="Barney" />\n&#x3C;MyComponentsWithProps name="Bam-bam" />\n&#x3C;MyComponentsWithProps name="Bety" />\n&#x3C;MyComponentsWithProps name="Wilma" />\n&#x3C;MyComponentsWithProps name="Pebbles" />\n</code></pre>\n<p>All of these components will display their name prop respectively. Each will have their own object created through the react ecosystem as we pass props DOWN to them that will look like this.</p>\n<p> Using props to get to know Bart</p>\n<ol>\n<li>Open up an html document and add in the proper dependancies</li>\n<li>Create an object with with name, identity, age and best_friend</li>\n<li>Set up a component that relys on the props from the <code>BartProfile</code> object</li>\n<li>Pass down the object as a prop called <code>bart</code> and mount it up</li>\n</ol>\n<pre><code>&#x3C;!DOCTYPE html>\n&#x3C;html lang="en">\n\n&#x3C;head>\n    &#x3C;title>Getting to know Bartholomew J. Simpson&#x3C;/title>\n&#x3C;/head>\n\n&#x3C;body>\n    &#x3C;div id="root">&#x3C;/div>\n\n    // Step 1\n    &#x3C;script crossorigin src="https://unpkg.com/react@16/umd/react.development.js">&#x3C;/script>\n    &#x3C;script crossorigin src="https://unpkg.com/react-dom@16/umd/react-dom.development.js">&#x3C;/script>\n    &#x3C;script crossorigin src="https://unpkg.com/babel-standalone@6.26.0/babel.js">&#x3C;/script>\n\n    &#x3C;script type="text/babel">\n\n        // Step 3\n        const BartComponent = props => {\n            return (\n                &#x3C;div>\n                &#x3C;h3>Name: {props.bart.name}&#x3C;/h3>\n                &#x3C;p> age: {props.bart.age}&#x3C;/p>\n                &#x3C;p>Identity: {props.bart.identity}&#x3C;/p>\n                &#x3C;p>Best Friend: {props.bart.best_friend}&#x3C;/p>\n                &#x3C;/div>\n            );\n            }\n\n        const App = () => {\n            \n            // Step 2\n            const BartProfile = {\n                name: "Bartholomew Jo-Jo Simpson",\n                identity: "The troublemaker of the Simpson family.",\n                age: "10 (2 years and 38 days older than Lisa)",\n                best_friend: \'Milhouse Van Houten\',\n            }\n\n            return\n                &#x3C;div>\n                    // Step 4\n                    &#x3C;BartComponent bart={BartProfile}>\n                &#x3C;/div>\n        }\n\n        ReactDom.render(&#x3C;App />, document.getElementById(\'root\'));\n    &#x3C;/script>\n\n&#x3C;/body>\n\n&#x3C;/html>\n</code></pre>\n<h3>Challenge</h3>\n<p>Build out a component called <code>&#x3C;MeComponent /></code> that will depend on Props being passed down to it. Those fields on the Props object will be descriptors about who you are as a person. Render out your <code>name</code>, <code>age</code>, <code>location</code>, <code>favoriteBook</code>, <code>favoriteBand</code>.</p>\n<h2>Using props to dynamically pass data to a React Component making it reusable in different contexts</h2>\n<ol>\n<li>Define a basic button and declare a className of <code>props.buttonStyles</code></li>\n<li>Pass the <code>&#x3C;Basicbutton /></code> into the <code>App</code></li>\n<li>Use defaultProps to declare the className of <code>props.buttonStyles</code> as teal</li>\n<li>Wrap <code>props.buttonStyles</code> with <code>backticks</code> and add basic styling to the button with a second class in a string</li>\n<li>Copy and paste more buttons into the <code>App</code> component</li>\n<li>Change the <code>buttonStyles</code> prop to red and create a new class in between the style tags</li>\n</ol>\n<pre><code>&#x3C;!DOCTYPE html>\n&#x3C;html lang="en">\n\n&#x3C;head>\n    &#x3C;title>Changing button colours with Reusability&#x3C;/title>\n\n    &#x3C;style>\n    // Inline styles go here\n\n    // Step 2b\n    .teal {\n        background: teal;\n    }\n\n    // Step 6b\n    .red {\n        background: red;\n    }\n\n    // Step 4b\n    .basicButtonStyles {\n        height: 40px;\n        width: 150px;\n        color: white;\n        font-size: 18px;\n    }\n\n    &#x3C;/style>\n\n&#x3C;/head>\n\n&#x3C;body>\n    &#x3C;div id="root">&#x3C;/div>\n\n    &#x3C;script crossorigin src="https://unpkg.com/react@16/umd/react.development.js">&#x3C;/script>\n    &#x3C;script crossorigin src="https://unpkg.com/react-dom@16/umd/react-dom.development.js">&#x3C;/script>\n    &#x3C;script crossorigin src="https://unpkg.com/babel-standalone@6.26.0/babel.js">&#x3C;/script>\n\n    &#x3C;script type="text/babel">\n\n        // Step 1                                       Step 4a\n        const BasicButton = props => &#x3C;button className={`basicButtonStyles ${props.buttonStyles}`}>Click me!&#x3C;/button>\n        // Step 2a\n        BasicButton.defaultProps = {\n            buttonStyles: \'teal\',\n        }\n\n        const App = () => {\n\n            return\n                &#x3C;div>\n                    &#x3C;h2>Click on a button&#x3C;/h2>\n                    // Step 2\n                    &#x3C;BasicButton />\n                    // Step 5\n                    &#x3C;BasicButton buttonStyles={red} />\n                    &#x3C;BasicButton />\n                    // Step 6a\n                    &#x3C;BasicButton buttonStyles={red} />\n                    &#x3C;BasicButton />\n                &#x3C;/div>\n        }\n\n        ReactDom.render(&#x3C;App />, document.getElementById(\'root\'));\n    &#x3C;/script>\n\n&#x3C;/body>\n\n&#x3C;/html>\n</code></pre>\n<h3>Challenge</h3>\n<p>Create dynamic text on some of the buttons with <code>props.buttonText</code></p>\n<h2>Implementing nested functional components</h2>\n<ul>\n<li>\n<p>All components return some sort of object at the end of the day.</p>\n</li>\n<li>\n<p>Being that we can use functions to return these objects, we can nest them together to make giant component trees.</p>\n</li>\n<li>\n<p>The props chain will flow top to bototn.</p>\n</li>\n<li>\n<p>As we pass data from one component to the next, we need to make sure that we reference the props in the proper manner. This pattern is called Prop Drilling...</p>\n</li>\n<li>\n<p>Create a html document with personData and a Parent component with props passed</p>\n</li>\n</ul>\n<pre><code>&#x3C;script type="text/babel">\n\n    const Parent = props => {\n        return (\n            &#x3C;h1>\n                Parent: {props.personData.parentName}\n            &#x3C;/h1>\n        )\n    };\n\n    const App = () => {\n        const personData = {\n            parentName: \'Gordon\',\n            childName: \'Mark\',\n            grandChildName: \'Blake\'\n        };\n\n        return (\n            &#x3C;div>\n                &#x3C;Parent personData={personData} />\n            &#x3C;/div>\n        );\n    };\n\n    ReactDom.render(&#x3C;App />, document.getElementById(\'root\'));\n\n&#x3C;/script>\n</code></pre>\n<ol start="2">\n<li>Create a <code>Child</code> component and pass it in to the <code>Parent</code> component</li>\n</ol>\n<pre><code>const Child = props => {\n        return (\n            &#x3C;div>\n                &#x3C;h2>Child: {props.personData.childName}&#x3C;/h2>\n            &#x3C;/div>\n        )\n    };\n\nconst Parent = props => {\n    return (\n        &#x3C;div>\n            &#x3C;h1>Parent: {props.personData.parentName}&#x3C;/h1>\n            &#x3C;Child personData={props.personData} />\n        &#x3C;/div>\n    )\n}\n</code></pre>\n<ol start="3">\n<li>The cycle continues for the <code>Grandhcild</code> component</li>\n</ol>\n<pre><code>const GrandChild = props => &#x3C;h3>Grandchild: {props.personData.childName}&#x3C;/h2>;\n\nconst Child = props => {\n    return (\n        &#x3C;div>\n            &#x3C;h2>Child: {props.personData.childName}&#x3C;/h2>\n            &#x3C;Grandchild personData={props.personData} />\n        &#x3C;/div>\n    )\n}\n</code></pre>\n<h3>Challenge</h3>\n<p>Create an object that represents a generational tree of your family similar to the <a href="https://learn.lambdaschool.com/fsw/module/reckyfgpdgasln9ci#follow-along-5">Simpsons Object</a> found in our follow along example. Keep it simple. Build out a few nested components that demonstrate the ability to pass data and conditionally render components if certain props are available.</p>',excerpt:"The way that data flows throughout a React app is crucial to understand when it comes to developing programs built in React. In React, when we pass our data around it’s passed around using State and Props. \n\n"}},{node:{id:"/Users/blakefletcher/projects/my-personal-blog/src/posts/react-week-1-day-3.md absPath of file >>> MarkdownRemark",frontmatter:{title:"How it works: React Week 1 Day 2 - Class components I",date:"June 13 2018"},fields:{slug:"/notes/react-week-1-day-3/"},html:'<p>Class Components are a big part of the React ecosystem because they bring a lot of functionality that we normally don’t get with the React API when using Functional Components.</p>\n<!-- end -->\n<p>Objectives:</p>\n<ul>\n<li>use a class component to render some state data to the DOM</li>\n<li>iterate over a list of of data and generate an array of React Components</li>\n<li>share data between components using state and props</li>\n<li>respond to events triggered by user interaction and handle user input via forms in React</li>\n</ul>\n<h2>Using a class component to render some state data to the DOM</h2>\n<p>The <code>React.Component</code> <em><strong>base</strong></em> class  allows us to tap into what we call the <code>Component Lifecycle</code>. Its methods give us control into how our components work, and if we’d like to use them, we have to build out a class component that extends the <code>React.Component</code> parent class:</p>\n<pre><code>class FooComponent extends React.Component {}\n</code></pre>\n<p>One integral part of creating components as classes is that you have the ability to set up a data object that your component might need to be concerned with by way using <code>state</code> as we call it and setting that object up on our <code>constructor</code> method. Once we have some data that we can render out to the DOM we need a vehicle that will allow us to render that data - this is achieved by returning some sort of JSX from within the life-cycle hook called <code>render</code>.</p>\n<ol>\n<li>Declare your class, and extend the React.Component Base class.</li>\n</ol>\n<pre><code>class FooComponent extends React.Component {\n</code></pre>\n<ol start="2">\n<li>Now we’ll set up our constructor and add state.</li>\n</ol>\n<pre><code>constructor() {\n  super();\n  this.state = {};\n}\n</code></pre>\n<ol start="3">\n<li>Render some UI and don’t forget to return some JSX</li>\n</ol>\n<pre><code>render() {\n  return &#x3C;div>Hello, I am Foo Component&#x3C;/div>;\n}\n</code></pre>\n<p>Our final component should look like this.</p>\n<pre><code>class FooComponent extends React.Component {\n  constructor() { \n    super();\n    this.state = {};\n  }\n  render() {\n    return &#x3C;div>Hello, I am Foo Component&#x3C;/div>;\n  }\n} \n</code></pre>\n<ol start="4">\n<li>Now lets add a property to our state data. Define a message property on the state object.</li>\n</ol>\n<pre><code>this.state = { \n  message: "Hello from App State!!"\n};\n</code></pre>\n<ol start="5">\n<li>Now we have that message on our Component’s state we can use it through interpolation. In our render method lets change the the message that we are currently printing out inside of that <code>div</code> to reference the state object. Remember the <code>this</code> keyword when pointing to an object on the Class constructor.</li>\n</ol>\n<pre><code>render() {\n  return &#x3C;div>{this.state.message}&#x3C;/div>;\n}\n</code></pre>\n<h3>Challenge</h3>\n<p>Lets take the functionality of this class component that we built earlier and extend it just a little bit. Declare a <code>Functional Component</code> called <code>RenderMessage</code> inside this <a href="https://codesandbox.io/s/103jkor46q">CodeSandbox</a>.</p>\n<ul>\n<li>Make sure you declare your Props Object that will be passed into this component.</li>\n<li>Return a <code>div</code> who’s child is <code>props.message</code></li>\n<li>Now inside of the <code>App</code> class pass in that <code>RenderMessage</code> component and pass down a message prop to <code>RenderMessage</code>. This message prop should be set equals to the message property on the state object.</li>\n<li>Once it’s all wired up properly you’ve done it!</li>\n</ul>\n<h2>Iterating over a list of data and generating an array of React Components</h2>\n<p>Lets look at the data we want to represent as DOM elements. We’ll render out a list of <code>Favorite Bands</code>.</p>\n<pre><code>const BandsData = [\n  { bandName: "Modest Mouse" },\n  { bandName: "Led Zeppelin" },\n  { bandName: "The Beatles" },\n  { bandName: "Guster" },\n  { bandName: "Kygo" },\n  { bandName: "Dear and the Headlights" },\n  { bandName: "Grizzly Bear" }\n];\n</code></pre>\n<p>We’ll give this new list to React and let it do it’s magic! This is all done with <code>.map</code>. Lets see the logic of this first before we apply it to a React Component. This should return us an array of <code>JSX</code> elements which are <code>divs</code> with the child of <code>band.bandName</code> interpolated as strings.</p>\n<pre><code>const newListOfData = BandsData.map(band => {\n  return &#x3C;div>{band.bandName}&#x3C;/div>;\n});\n</code></pre>\n<p>Now lets apply this to a React Component.</p>\n<pre><code>const App = () => {\n  return (\n    &#x3C;div className="App">\n      {BandsData.map(band => {\n        return &#x3C;div>{band.bandName}&#x3C;/div>;\n      })}\n    &#x3C;/div>\n  );\n};\n</code></pre>\n<p>To make this work, there a few more steps:</p>\n<ol>\n<li>Take the list and set in on state</li>\n<li>Check your state by console.logging</li>\n<li><code>Map</code> over <code>bands</code> and return all wrapped in <code>divs</code> with <code>band.bandName</code> and <code>band.albumName</code></li>\n<li>Give each child a unique <code>key</code> prop</li>\n</ol>\n<pre><code>import React from "react";\nimport ReactDOM from "react-dom";\n\nimport "./styles.css";\n\nconst BandsData = [\n  { id: 0, bandName: "Led Zeppelin", albumName: "Led Zeppelin II" },\n  { id: 1, bandName: "The Beatles", albumName: "Sgt. Peppers Lonely Heart Clubs Band" },\n  { id: 2, bandName: "Grizzly Bear", albumName: "Veckatimist" }\n];\n\nclass App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      name: "music",\n      // Step 1\n      bands: BandsData\n    };\n  }\n  render() {\n      // Step 2\n    console.log(this.state);\n    return (\n      &#x3C;div className="App">\n        &#x3C;h1>My favourite {this.state.name}&#x3C;/h1>\n        // Step 3\n        {this.state.bands.map(band => {\n                // Step 4\n          return &#x3C;div key={band.id}>{band.bandName}: {band.albumName} &#x3C;/div>\n        })}\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<h2>Sharing data between components using state and props</h2>\n<p>We are nothing in React if we can’t pass state around from one component to another. The way that we achieve this is this idea of <code>state to props</code>. Whatever is set on the state of our <code>Components</code> can be shared between components by passing it down as props.</p>\n<p>State is as persistent as long as the component is on the screen, we can use it to hold on to memory for our application. That memory could be any data that we pull in from a server elsewhere, some edits from a submission form or simply just based on Users interacting with your web page.</p>\n<p>Remember that state is just an object that we have access to which lives on the <code>class component</code>\'s constructor. What’s great about this state object, is that when it changes our component will re-render. We can also pass whatever data is found on this state object around as props to other components.</p>\n<ol>\n<li>Create a <code>Message</code> component in <code>./components/MessageComponent</code> (steps are the same for the <code>Name</code> component)</li>\n</ol>\n<pre><code>import React from "react";\n\nconst Message = props => &#x3C;h1>{props.propsMessage}&#x3C;/h1>;\n\nexport default Message;\n\n// Name component\n\nimport React from "react";\n\nconst Name = props => &#x3C;h1>My name is {props.propsName}&#x3C;/h1>;\n\nexport default Name;\n</code></pre>\n<ol start="2">\n<li>Import it with <code>import Message from "./components/MessageComponent";</code></li>\n<li>Add a <code>message</code> to <code>this.state</code></li>\n<li>Bring in the <code>Message</code> component to App and pass down the message with <code>propsMessage={this.state.message}</code> (notice propsMessage is the same as the component above)</li>\n</ol>\n<pre><code>import React from "react";\nimport ReactDOM from "react-dom";\n\nimport "./styles.css";\n// Step 2\nimport Message from "./components/MessageComponent";\n\nclass App extends React.Component {\n  constructor() {\n    super();\n    // Step 3\n    this.state = {\n      message: "G\'day, monsieur!",\n      name: "Blake"\n    };\n  }\n\n  render() {\n    return (\n      &#x3C;div className="App">\n      // Step 4\n        &#x3C;Message propsMessage={this.state.message} />\n        &#x3C;Name propsName={this.state.name} />\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<p>Remember, state is mutable. You can change it with a function called <code>setState()</code>. This is how it works with an input form</p>\n<ol>\n<li>Change <code>message</code> in state to an empty string</li>\n</ol>\n<pre><code>this.state = {\n      message: "",\n      name: "Blake"\n    };\n</code></pre>\n<ol start="2">\n<li>Create a <code>handleInputChange</code> function that uses <code>setState</code> to set the value of <code>message</code> with whatever is in the input field</li>\n</ol>\n<pre><code>  handleInputChange = event => {\n    this.setState({message: event.target.value})\n  }\n</code></pre>\n<ol start="3">\n<li>Add an <code>input</code> div to App and onChange set it to <code>this.handleInputChange</code> (notice its not in state so it doesn\'t have this.state)</li>\n</ol>\n<p>Remember, the message component is receiving the changes from this.setState. In the end, the file will look like this:</p>\n<pre><code>import React from "react";\nimport ReactDOM from "react-dom";\n\nimport "./styles.css";\nimport Message from "./components/MessageComponent";\nimport Name from "./components/NameComponent";\n\nclass App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      message: "",\n      name: "Blake"\n    };\n  }\n\n  handleInputChange = event => {\n    this.setState({message: event.target.value})\n  }\n\n  render() {\n    return (\n      &#x3C;div className="App">\n        &#x3C;Message propsMessage={this.state.message} />\n        &#x3C;Name propsName={this.state.name} />\n        &#x3C;input onChange={this.handleInputChange} />\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<h2>Responding to events triggered by user interaction and handle user input via forms in React</h2>\n<p>In Web development everything you do is based around the <code>Document Object Model (DOM)</code>. The DOM has built into it an event loop that listens for changes across some part of the UI. To access that event loop in the Virtual DOM the react team put together what is called the <code>Synthetic Event</code> so that we can pretty much have access to all the events we would need to provide users with an interactive web application. <em><strong>Learn to harness the power of events in React and you’ll be set to build out React Web Applications.</strong></em></p>\n<p>How do we take in user input via forms, respond to clicks, mouse events and scrolling? Here is a statement straight from the <a href="https://reactjs.org/docs/handling-events.html" title="Handling events">React Docs</a></p>\n<p>Handling events with React elements is very similar to handling events on DOM elements. There are some syntactic differences:</p>\n<ul>\n<li>React events are named using camelCase, rather than lowercase.</li>\n<li>With JSX you pass a function as the event handler, rather than a string.</li>\n</ul>\n<p>Below are three examples of different handlers (<code>onClick</code>, <code>onDoubleClick</code>, <code>onChange</code>) and different outputs (<code>alert</code>, <code>console.log</code>, <code>this.setState</code>)</p>\n<pre><code>import React from "react";\nimport ReactDOM from "react-dom";\n\nimport "./styles.css";\n\nclass App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      someValue: \'\',\n    }\n  }  \n\n  clickHandler = () => alert("Single Click!");\n  doubleClickHandler = () => console.log("Double Clicked!");\n  changeHandler = event => {\n    this.setState({ someValue: event.target.value });\n  };\n\n  render() {\n    return (\n      &#x3C;div className="App">\n        &#x3C;h1>Hello Handlers&#x3C;/h1>\n        &#x3C;h2>Lets build out some handler functions.&#x3C;/h2>\n        &#x3C;button onClick={this.clickHandler}>Click Handler Demo&#x3C;/button>\n        &#x3C;button onDoubleClick={this.doubleClickHandler}>Double Click Handler&#x3C;/button>\n        &#x3C;input onChange={this.changeHandler} placeholder="Change my input" />\n        &#x3C;p>To get this output: {this.state.someValue}&#x3C;/p>\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<h3>Two projects</h3>\n<p><a href="https://github.com/LambdaSchool/React-Todo">React Todo</a></p>\n<p><a href="https://github.com/LambdaSchool/React-Sorting-Hat">React-Sorting-Hat</a></p>',
excerpt:"\nClass Components are a big part of the React ecosystem because they bring a lot of functionality that we normally don’t get with the React API when using Functional Components.\n\n"}},{node:{id:"/Users/blakefletcher/projects/my-personal-blog/src/posts/react-week-1-day-4.md absPath of file >>> MarkdownRemark",frontmatter:{title:"How it works: React Week 1 Day 4: Class components II",date:"June 14 2018"},fields:{slug:"/notes/react-week-1-day-4/"},html:'<p>This module is about the intricicies of how we operate within a React Class Component, how to build out an application that allows you to perform basic operations to a data set, and display changes to that data set to the screen.</p>\n<!-- end -->\n<p>Objectives:</p>\n<ul>\n<li>create, read and update a component\'s state object</li>\n<li>of  demonstrate two ways to properly use the <code>setState</code> function and describe the rules of it</li>\n<li>implement reusable and nested class components</li>\n</ul>\n<h2>Creating reading and updating a component\'s state object</h2>\n<p>You can think of state as the data your components need, and the ability your components have to update/access data that would need to be displayed by the component itself. Learning to use the <code>state</code> object and <code>setState</code> function will help you gain an understanding as to why we have React and what it’s really good at doing.</p>\n<p><a href="https://reactjs.org/docs/state-and-lifecycle.html">State and lifecycle explained in the React Docs</a></p>\n<p>If we simply passed down some items to a component via props on an array for example, we wouldn’t really have a way for us to manipulate and update that information as needed.</p>\n<p>React gives us a tool to work with the <code>state</code> object that we put on our constructor function. This tool is called <code>setState</code> and it lives on the <code>Component</code> base-class as a method that we can tap into in order to make changes to state. Note calling setState will for React to trigger an invocation (call) to <code>render</code>. This is how react is reactive. Any time we change our state, our component gets re-rendered and our changes are immediately reflected on the screen.</p>\n<pre><code>class App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      [STATE_OBJECT]: ""\n    };\n  }\n\nhandleInputChange = (event) => {\n  this.setState({ [STATE_OBJECT]: event.target.value });\n}\n\nrender() {\n    return (\n      &#x3C;div>\n        &#x3C;div>{this.state.[STATE_OBJECT]}&#x3C;/div>\n        &#x3C;input\n            type="text"\n            placeholder="change the [STATE_OBJECT]"\n            onChange={this.handleInputChange}\n        />\n      &#x3C;/div>\n    );\n  }\n\n}\n</code></pre>\n<p>This input field is pretty trivial, and demonstrates the use of <code>setState</code> pretty well, however, we’re trying to achieve the idea of <code>reusability</code>. So lets take a look at what happens to our input handler if we bind it to our input in a <code>controlled</code> fashion. One can create state from scratch.</p>\n<pre><code>class App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n    };\n  }\n\n  onClickMe = () => {\n    this.setState({message: "Clicked"})\n  }\n\n  render() {\n    return (\n      &#x3C;div className="App">\n        &#x3C;h1>Updating State via `SetState`&#x3C;/h1>\n        &#x3C;p>{this.state.message}&#x3C;/p>\n        &#x3C;button onClick={this.onClickMe}>Click me&#x3C;/button>\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<p>Changing the state of a boolean and conditionally rendering components is when things get interesting.</p>\n<pre><code>class LoginControl extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = {isLoggedIn: false};\n  }\n\n  handleLoginClick() {\n    this.setState({isLoggedIn: true});\n  }\n\n  handleLogoutClick() {\n    this.setState({isLoggedIn: false});\n  }\n\n  render() {\n    const isLoggedIn = this.state.isLoggedIn;\n    let button;\n\n    if (isLoggedIn) {\n      button = &#x3C;LogoutButton onClick={this.handleLogoutClick} />;\n    } else {\n      button = &#x3C;LoginButton onClick={this.handleLoginClick} />;\n    }\n\n    return (\n      &#x3C;div>\n        &#x3C;Greeting isLoggedIn={isLoggedIn} />\n        {button}\n      &#x3C;/div>\n    );\n  }\n}\n\nfunction UserGreeting(props) {\n  return &#x3C;h1>Welcome back!&#x3C;/h1>;\n}\n\nfunction GuestGreeting(props) {\n  return &#x3C;h1>Please sign up.&#x3C;/h1>;\n}\n\nfunction Greeting(props) {\n  const isLoggedIn = props.isLoggedIn;\n  if (isLoggedIn) {\n    return &#x3C;UserGreeting />;\n  }\n  return &#x3C;GuestGreeting />;\n}\n\nfunction LoginButton(props) {\n  return (\n    &#x3C;button onClick={props.onClick}>\n      Login\n    &#x3C;/button>\n  );\n}\n\nfunction LogoutButton(props) {\n  return (\n    &#x3C;button onClick={props.onClick}>\n      Logout\n    &#x3C;/button>\n  );\n}\n</code></pre>\n<h2>Demonstrating two ways to use setState (and describing its rules)</h2>\n<p><code>setState</code> can be async. There is a step between when we call <code>setState</code> in which our component changes are <code>diffed</code> through a process called <code>reconciliation</code>. Lets say we have a user that types in an input field: (1) User types in input field, (2) Reconciliation, (3) Input Component, (4) setState invoked, (5) Input Component re-renders</p>\n<p>If I had some state that I was expecting to be changed by multiple calls to the <code>setState</code> function, it wouldn’t behave the way that I may have initially intended it to.</p>\n<pre><code>counterByThree = () => {\n  this.setState({ count: this.state.count + 1 });\n  this.setState({ count: this.state.count + 1 });\n  this.setState({ count: this.state.count + 1 });\n};\n</code></pre>\n<p>React will simply ignore a couple of the calls by using this process called batching. This will result in only the last call to setState actually taking place. The solution is using functional setState (the ability to pass a callback function to our setState function).</p>\n<pre><code>counterByThree = () => {\n  this.setState(prevState => ({ count: prevState.count + 1 })),\n    this.setState(prevState => ({ count: prevState.count + 1 })),\n    this.setState(prevState => ({ count: prevState.count + 1 }));\n};\n</code></pre>\n<p>This way solves both of our problems. We have controlled the batching of these calls to <code>setState</code> by returning a <code>Queue</code> of calls in this fashion. We are also reading from the <code>previous copy</code> of state rather than the current copy.</p>\n<h2>Implimenting reusable and nested class components</h2>\n<p>The idea that you could build a single component, and share it all over your application is one that React does really well. Because components can be singular representations of UI, you can achieve reusability rather simply and eloquently. This will help your applications grow and scale in a wonderful way.</p>\n<p>A navigation bar may have a container, and multiple buttons/links, some logo. You’d need to pass these children elements into the navbar container you build right? It’s the same with react, we’ll build some complex piece of UI that we can nest together using the <code>compositional paradigm</code>.</p>\n<p><a href="https://reactjs.org/docs/composition-vs-inheritance.html">Composition and inheritance explained in the React Docs</a></p>\n<p><a href="https://codesandbox.io/s/qvkrx4zm56">https://codesandbox.io/s/qvkrx4zm56</a></p>\n<ol>\n<li>Change the parent of the child to a Class component: (1) Change const to class, (2) Add CCR, (3) Add this to props, (4) console.log(props)</li>\n</ol>\n<pre><code>const City = props => {\n  return (\n    &#x3C;div>\n      {props.city.city}, {props.city.state}{" "}\n      &#x3C;Zips zipList={props.city.zip_codes} />{" "}\n    &#x3C;/div>\n  );\n};\n\n// becomes\n\nclass City extends React.Component {\n  constructor(props) {\n    console.log(props);\n    super(props)\n    // this.state goes here\n  }\n  render() {\n  return (\n    &#x3C;div>\n      {this.props.city.city}, {this.props.city.state}{" "}\n      &#x3C;Zips zipList={this.props.city.zip_codes} />{" "}\n    &#x3C;/div>\n  );\n  }\n};\n</code></pre>\n<ol start="2">\n<li>Add zips to the state and console.log under the render function to make sure they\'re getting passed down</li>\n</ol>\n<pre><code>this.state = {\n      zips: props.city.zip\n    }\n\nrender() {\n    console.log(this.state);\n</code></pre>\n<ol start="3">\n<li>Change the child component into a stateless functional component</li>\n</ol>\n<pre><code>import React from "react";\n\nclass Zip extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      zips: []\n    };\n  }\n  render() {\n    // Lets build this out together!\n    return &#x3C;div>Zippity&#x3C;/div>;\n  }\n}\nexport default Zip;\n\n// becomes\n\nimport React from "react";\n\nconst Zip = props => {\n    // Lets build this out together!\n    return &#x3C;div>Zippity&#x3C;/div>;\n  }\n\nexport default Zip;\n</code></pre>\n<ol start="4">\n<li>Map over the <code>zips</code> in <code>state</code> of the <code>City</code> component</li>\n</ol>\n<pre><code>class City extends React.Component {\n  constructor(props) {\n    console.log(props);\n    super(props);\n    this.state = {\n      zips: props.city.zip\n    };\n  }\n  render() {\n    console.log(this.state);\n    return (\n      &#x3C;div>\n        {this.props.city.city}, {this.props.city.state}{" "}\n        {this.state.zips.map(zip => &#x3C;Zip zipCode={zip} key={zip} />)}\n      &#x3C;/div>\n    );\n  }\n}\n</code></pre>\n<ol start="5">\n<li>return <code>props.zipCode</code> on the <code>Zip</code> component</li>\n</ol>\n<pre><code>const Zip = props => {\n    return &#x3C;div>{props.zipCode}&#x3C;/div>;\n  }\n</code></pre>\n<h4>Here is a <a href="https://www.youtube.com/watch?time_continue=983&#x26;v=zlBDC1-hf-s">recap</a></h4>',excerpt:"\nThis module is about the intricicies of how we operate within a React Class Component, how to build out an application that allows you to perform basic operations to a data set, and display changes to that data set to the screen.\n\n"}}]}},pathContext:{}}}});
//# sourceMappingURL=path---index-ae6acc73eb0975bcc0e2.js.map