webpackJsonp([0x785a1b41064b],{339:function(e,t){e.exports={data:{markdownRemark:{html:'<p>This module is about the intricicies of how we operate within a React Class Component, how to build out an application that allows you to perform basic operations to a data set, and display changes to that data set to the screen.</p>\n<!-- end -->\n<p>Objectives:</p>\n<ul>\n<li>create, read and update a component\'s state object</li>\n<li>of  demonstrate two ways to properly use the <code>setState</code> function and describe the rules of it</li>\n<li>implement reusable and nested class components</li>\n</ul>\n<h2>Creating reading and updating a component\'s state object</h2>\n<p>You can think of state as the data your components need, and the ability your components have to update/access data that would need to be displayed by the component itself. Learning to use the <code>state</code> object and <code>setState</code> function will help you gain an understanding as to why we have React and what it’s really good at doing.</p>\n<p><a href="https://reactjs.org/docs/state-and-lifecycle.html">State and lifecycle explained in the React Docs</a></p>\n<p>If we simply passed down some items to a component via props on an array for example, we wouldn’t really have a way for us to manipulate and update that information as needed.</p>\n<p>React gives us a tool to work with the <code>state</code> object that we put on our constructor function. This tool is called <code>setState</code> and it lives on the <code>Component</code> base-class as a method that we can tap into in order to make changes to state. Note calling setState will for React to trigger an invocation (call) to <code>render</code>. This is how react is reactive. Any time we change our state, our component gets re-rendered and our changes are immediately reflected on the screen.</p>\n<pre><code>class App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      [STATE_OBJECT]: ""\n    };\n  }\n\nhandleInputChange = (event) => {\n  this.setState({ [STATE_OBJECT]: event.target.value });\n}\n\nrender() {\n    return (\n      &#x3C;div>\n        &#x3C;div>{this.state.[STATE_OBJECT]}&#x3C;/div>\n        &#x3C;input\n            type="text"\n            placeholder="change the [STATE_OBJECT]"\n            onChange={this.handleInputChange}\n        />\n      &#x3C;/div>\n    );\n  }\n\n}\n</code></pre>\n<p>This input field is pretty trivial, and demonstrates the use of <code>setState</code> pretty well, however, we’re trying to achieve the idea of <code>reusability</code>. So lets take a look at what happens to our input handler if we bind it to our input in a <code>controlled</code> fashion. One can create state from scratch.</p>\n<pre><code>class App extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n    };\n  }\n\n  onClickMe = () => {\n    this.setState({message: "Clicked"})\n  }\n\n  render() {\n    return (\n      &#x3C;div className="App">\n        &#x3C;h1>Updating State via `SetState`&#x3C;/h1>\n        &#x3C;p>{this.state.message}&#x3C;/p>\n        &#x3C;button onClick={this.onClickMe}>Click me&#x3C;/button>\n      &#x3C;/div>\n    );\n  }\n}\n\nconst rootElement = document.getElementById("root");\nReactDOM.render(&#x3C;App />, rootElement);\n</code></pre>\n<p>Changing the state of a boolean and conditionally rendering components is when things get interesting.</p>\n<pre><code>class LoginControl extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = {isLoggedIn: false};\n  }\n\n  handleLoginClick() {\n    this.setState({isLoggedIn: true});\n  }\n\n  handleLogoutClick() {\n    this.setState({isLoggedIn: false});\n  }\n\n  render() {\n    const isLoggedIn = this.state.isLoggedIn;\n    let button;\n\n    if (isLoggedIn) {\n      button = &#x3C;LogoutButton onClick={this.handleLogoutClick} />;\n    } else {\n      button = &#x3C;LoginButton onClick={this.handleLoginClick} />;\n    }\n\n    return (\n      &#x3C;div>\n        &#x3C;Greeting isLoggedIn={isLoggedIn} />\n        {button}\n      &#x3C;/div>\n    );\n  }\n}\n\nfunction UserGreeting(props) {\n  return &#x3C;h1>Welcome back!&#x3C;/h1>;\n}\n\nfunction GuestGreeting(props) {\n  return &#x3C;h1>Please sign up.&#x3C;/h1>;\n}\n\nfunction Greeting(props) {\n  const isLoggedIn = props.isLoggedIn;\n  if (isLoggedIn) {\n    return &#x3C;UserGreeting />;\n  }\n  return &#x3C;GuestGreeting />;\n}\n\nfunction LoginButton(props) {\n  return (\n    &#x3C;button onClick={props.onClick}>\n      Login\n    &#x3C;/button>\n  );\n}\n\nfunction LogoutButton(props) {\n  return (\n    &#x3C;button onClick={props.onClick}>\n      Logout\n    &#x3C;/button>\n  );\n}\n</code></pre>\n<h2>Demonstrating two ways to use setState (and describing its rules)</h2>\n<p><code>setState</code> can be async. There is a step between when we call <code>setState</code> in which our component changes are <code>diffed</code> through a process called <code>reconciliation</code>. Lets say we have a user that types in an input field: (1) User types in input field, (2) Reconciliation, (3) Input Component, (4) setState invoked, (5) Input Component re-renders</p>\n<p>If I had some state that I was expecting to be changed by multiple calls to the <code>setState</code> function, it wouldn’t behave the way that I may have initially intended it to.</p>\n<pre><code>counterByThree = () => {\n  this.setState({ count: this.state.count + 1 });\n  this.setState({ count: this.state.count + 1 });\n  this.setState({ count: this.state.count + 1 });\n};\n</code></pre>\n<p>React will simply ignore a couple of the calls by using this process called batching. This will result in only the last call to setState actually taking place. The solution is using functional setState (the ability to pass a callback function to our setState function).</p>\n<pre><code>counterByThree = () => {\n  this.setState(prevState => ({ count: prevState.count + 1 })),\n    this.setState(prevState => ({ count: prevState.count + 1 })),\n    this.setState(prevState => ({ count: prevState.count + 1 }));\n};\n</code></pre>\n<p>This way solves both of our problems. We have controlled the batching of these calls to <code>setState</code> by returning a <code>Queue</code> of calls in this fashion. We are also reading from the <code>previous copy</code> of state rather than the current copy.</p>\n<h2>Implimenting reusable and nested class components</h2>\n<p>The idea that you could build a single component, and share it all over your application is one that React does really well. Because components can be singular representations of UI, you can achieve reusability rather simply and eloquently. This will help your applications grow and scale in a wonderful way.</p>\n<p>A navigation bar may have a container, and multiple buttons/links, some logo. You’d need to pass these children elements into the navbar container you build right? It’s the same with react, we’ll build some complex piece of UI that we can nest together using the <code>compositional paradigm</code>.</p>\n<p><a href="https://reactjs.org/docs/composition-vs-inheritance.html">Composition and inheritance explained in the React Docs</a></p>\n<p><a href="https://codesandbox.io/s/qvkrx4zm56">https://codesandbox.io/s/qvkrx4zm56</a></p>\n<ol>\n<li>Change the parent of the child to a Class component: (1) Change const to class, (2) Add CCR, (3) Add this to props, (4) console.log(props)</li>\n</ol>\n<pre><code>const City = props => {\n  return (\n    &#x3C;div>\n      {props.city.city}, {props.city.state}{" "}\n      &#x3C;Zips zipList={props.city.zip_codes} />{" "}\n    &#x3C;/div>\n  );\n};\n\n// becomes\n\nclass City extends React.Component {\n  constructor(props) {\n    console.log(props);\n    super(props)\n    // this.state goes here\n  }\n  render() {\n  return (\n    &#x3C;div>\n      {this.props.city.city}, {this.props.city.state}{" "}\n      &#x3C;Zips zipList={this.props.city.zip_codes} />{" "}\n    &#x3C;/div>\n  );\n  }\n};\n</code></pre>\n<ol start="2">\n<li>Add zips to the state and console.log under the render function to make sure they\'re getting passed down</li>\n</ol>\n<pre><code>this.state = {\n      zips: props.city.zip\n    }\n\nrender() {\n    console.log(this.state);\n</code></pre>\n<ol start="3">\n<li>Change the child component into a stateless functional component</li>\n</ol>\n<pre><code>import React from "react";\n\nclass Zip extends React.Component {\n  constructor() {\n    super();\n    this.state = {\n      zips: []\n    };\n  }\n  render() {\n    // Lets build this out together!\n    return &#x3C;div>Zippity&#x3C;/div>;\n  }\n}\nexport default Zip;\n\n// becomes\n\nimport React from "react";\n\nconst Zip = props => {\n    // Lets build this out together!\n    return &#x3C;div>Zippity&#x3C;/div>;\n  }\n\nexport default Zip;\n</code></pre>\n<ol start="4">\n<li>Map over the <code>zips</code> in <code>state</code> of the <code>City</code> component</li>\n</ol>\n<pre><code>class City extends React.Component {\n  constructor(props) {\n    console.log(props);\n    super(props);\n    this.state = {\n      zips: props.city.zip\n    };\n  }\n  render() {\n    console.log(this.state);\n    return (\n      &#x3C;div>\n        {this.props.city.city}, {this.props.city.state}{" "}\n        {this.state.zips.map(zip => &#x3C;Zip zipCode={zip} key={zip} />)}\n      &#x3C;/div>\n    );\n  }\n}\n</code></pre>\n<ol start="5">\n<li>return <code>props.zipCode</code> on the <code>Zip</code> component</li>\n</ol>\n<pre><code>const Zip = props => {\n    return &#x3C;div>{props.zipCode}&#x3C;/div>;\n  }\n</code></pre>\n<h4>Here is a <a href="https://www.youtube.com/watch?time_continue=983&#x26;v=zlBDC1-hf-s">recap</a></h4>',frontmatter:{title:"How it works: React Week 1 Day 4: Class components II",date:"June 14 2018"}}},pathContext:{slug:"/notes/react-week-1-day-4/"}}}});
//# sourceMappingURL=path---notes-react-week-1-day-4-dea399e0a231e0ed847b.js.map